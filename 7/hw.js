var TArray = function(type) {
	// type - string

	// переопределить:
	// push -> arguments
	// unshift
	// splice
	// concat
};
TArray.prototype = [];

var nArray = new TArray("number");
var bArray = new TArray("boolean");